<!DOCTYPE html>
<html>
<head>
  <title>開発中のアプリ</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>

<div class='header'>
  <header>
    <%=link_to( image_tag( '/images/title.png', :size => '160x75', ), '/') %>
    <% if user_signed_in? %>
      <%= link_to "#{current_user.email}をログアウト", destroy_user_session_path, method: :delete %>
    <% else %>
      <%= link_to "ログイン", new_user_session_path %>
    <% end %>
  </header>
</div>

<div class='body'>
  <p class="notice"><%= notice %></p>
  <p class="alert"><%= alert %></p>
  <%= yield %>
</div>

<div class='footer'>
  <footer>
    <%=link_to( 'on github', 'https://github.com/mitsuhirookuno/Rails4Tutorial') %>
    <%=link_to( 'on jenkins', 'http://jenkins.rubyprogrammer.info/job/Rails4Tutorial/') %>
    <%=Time.now.strftime('%Y/%m/%d %H:%M:%S') %>
  </footer>
</div>

</body>
</html>
